ChatGPT5.1より

例えば：

ウィンドウタイトルが取れているか

コントロールが存在するか

Pywinautoが正しいツリーを認識するか

こういう「事前チェック関数」ならCodexは書ける。

def check_dialog_exists():
    try:
        app = Application(backend="uia").connect(title="名前を付けて保存", timeout=3)
        return True
    except:
        return False


これを作って実行すれば、君は実際に保存しなくても「ウィンドウを認識できるか」くらいはすぐ確認できる。